//program to check if a number is odd or even
#include <stdio.h>
int main(void) {
int number;
printf("enter number");
scanf("%d",&number);
printf("%d",number%2==0);
//if true then even if false then odd

    return 0;
}
