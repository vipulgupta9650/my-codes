#include <stdio.h>
int main(void) {
int age;
printf("enter age");
scanf("%d",&age);
age == 18 ? printf("age is 18"):printf("age is not 18");

    return 0;
}
//in case we have only one condition in  ternary operator that age is 18 then we have to use double equal to