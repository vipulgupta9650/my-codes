// ternary operator
#include <stdio.h>
int main(void){
int age;
printf("enter age");
scanf("%d",&age);
age >= 18 ? printf("adult"):printf("not adult");
return 0;
}
// ternary operator can be used only when we have single line statement
